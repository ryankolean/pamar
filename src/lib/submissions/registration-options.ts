/** Options shared by the subcontractor registration form (client) and its validation. */
export const businessCertifications = ["DBE", "MBE", "WBE", "SBE", "VOSB / SDVOSB"] as const;

export const bondingCapacities = [
  "Not bonded",
  "Up to $250K",
  "$250K–$1M",
  "$1M–$5M",
  "More than $5M",
] as const;
