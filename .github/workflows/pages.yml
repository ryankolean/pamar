name: Preview (GitHub Pages)

# Publishes the client preview to https://ryankolean.github.io/pamar on every push to main.
# It is a static export (see next.config.ts) in preview mode: sample content, no search
# indexing, forms accepted but not delivered. There is no password on GitHub Pages.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          STATIC_EXPORT: "1"
          SITE_MODE: preview
          NEXT_PUBLIC_BASE_PATH: /pamar
          NEXT_PUBLIC_SITE_URL: https://ryankolean.github.io/pamar
      - run: touch out/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
